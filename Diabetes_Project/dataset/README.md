# Dataset Folder

- `diabetes_brfss_binary.csv` is the primary runtime dataset.
- `legacy/` contains the original synthetic datasets that are no longer used for training.
- `archive (5)` remains outside runtime and is used only as a source/reference folder.
